<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.mes.jss.basic.mapper.CommonMapper">

  	<!-- 사원 전체 리스트 -->
  	<select id="getListComm" resultType="CommVO">
  		select  comm_code, comm_name, comm_note, insert_date, modify_date
  		from  common
  	    order by comm_code

  	</select>
	
	<!-- 사원번호 시퀀스 -->
	<select id="getListDeta" resultType="CommVO">
			select deta_code, deta_name, deta_note, deta_insert_date, deta_modify_date
			from details_comm
			where comm_code = #{commCode}
	</select>
	
	<!-- 사원등록 -->  	
	<insert id="insertEmp" parameterType="EmpVO">
		insert into emp (emp_no, emp_name, phone_num, email, hire_date, password, role_id)
		       values(#{empNo}, #{empName}, #{phoneNum}, #{email}, to_date(#{hireDate},'yyyy/MM/DD'), #{password}, 'ROLE_USER')
	</insert>
	
	<select id="selectEmp" parameterType="EmpVO" resultType="EmpVO">
		select * from emp where emp_no = #{empNo}
	</select>
	
	<update id="updateEmp" parameterType="EmpVO">
		update emp
		set position = #{position} ,
		    dept_name = #{deptName} ,
		    role_id = #{roleId}
	    where emp_no = #{empNo}
	</update>
	
	<delete id="deleteEmp">
		delete from emp where emp_no = #{empNo}
	</delete>
	
	<select id="getUser" resultType="EmpVO">
		select * from emp where emp_no = #{empNo}
	</select>
	
  </mapper>